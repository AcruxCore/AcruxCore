meta {
  name: Remove Alias Binding
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/prompts/{{promptId}}/aliases/{{aliasName}}/tools/{{toolId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}
