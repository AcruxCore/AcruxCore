meta {
  name: List Versions
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/prompts/{{promptId}}/versions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

params:query {
  ~page: 1
  ~limit: 20
}
