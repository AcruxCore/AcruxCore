meta {
  name: Export Version
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/prompts/{{promptId}}/versions/{{versionNumber}}/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}
