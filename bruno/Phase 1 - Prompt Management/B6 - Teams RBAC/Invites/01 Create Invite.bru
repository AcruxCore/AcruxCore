meta {
  name: Create Invite
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/teams/{{teamId}}/invites
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "role": "editor"
  }
}
