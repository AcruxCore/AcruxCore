meta {
  name: Update Member Role
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/v1/teams/{{teamId}}/members/{{memberId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "role": "editor"
  }
}
