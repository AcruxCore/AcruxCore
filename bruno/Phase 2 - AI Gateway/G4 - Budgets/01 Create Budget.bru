meta {
  name: Create Budget
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/gateway/budgets
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "period": "month",
    "limitUsd": 50
  }
}
