meta {
  name: Execute Tool
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/tools/{{toolId}}/execute
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "arguments": {"city": "Berlin"}
  }
}
