meta {
  name: Get Trace Settings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/traces/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

assert {
  res.status: eq 200
}
