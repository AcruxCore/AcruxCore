meta {
  name: Get Session
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

assert {
  res.status: eq 200
}
